
# � How To Run

1.  **Clone the Repo**:
    ```bash
    git clone <repo-url>
    cd IBeLuLu
    ```

2.  **Add Secrets**:
    Create a `.env.local` file in the main `IBeLuLu` folder with your keys.

3.  **Run**:
    ```bash
    cd docker
    docker-compose up
    ```

**Frontend:** http://localhost:5173
